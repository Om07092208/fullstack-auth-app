services:
  backend:
    build:
      context: ./backend
    container_name: auth-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: auth-frontend
    depends_on:
      - backend
    ports:
      - "3000:3000"
    restart: unless-stopped
